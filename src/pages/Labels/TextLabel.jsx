import React from "react";

const TextLabel = ({
    label
}) => {
  return (
    <label className="text-[#45474b] text-[16px] font-semibold">
      {label}
    </label>
  );
};

export default TextLabel;
